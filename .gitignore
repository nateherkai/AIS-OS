.env
.env.local
.DS_Store
*.log
__pycache__/
node_modules/
.claude/settings.local.json
audits/

# Gmail OAuth (never commit)
dashboard/.gmail-credentials.json
dashboard/.gmail-token.json

# Dream output — derived from local Claude transcripts. May contain prompt hashes,
# session IDs, and snippets. Generated nightly; do not commit.
dashboard/data/dreams/*.json
!dashboard/data/dreams/.gitkeep

# AIS-OS lint report — derived runtime artifact
dashboard/lint-report.md

# Dream card images — generated by OpenAI gpt-image-1, not committed
dashboard/data/dreams/images/
